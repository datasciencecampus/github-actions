name: Test Add Pull Request To Projects Reusable Workflow

'on':
  pull_request:
    types:
      - opened
      - reopened
  workflow_dispatch:
    inputs:
      project_field_values:
        description: >-
          JSON list of mappings, for example
          [{"project":1234,"field":"Status","value":"Review"}]
        required: true
        type: string
      pull_request_node_id:
        description: Pull request node id for a manual test run.
        required: true
        type: string

permissions:
  contents: read

jobs:
  test-add-pr-to-projects-reusable:
    uses: ./.github/workflows/add-pr-to-projects.yml
    secrets:
      PROJECT_ROUTER_BOT_PRIVATE_KEY: ${{ secrets.PROJECT_ROUTER_BOT_PRIVATE_KEY }}
    with:
      repository: ${{ github.event.repository.name }}
      pull_request_node_id: ${{ inputs.pull_request_node_id || github.event.pull_request.node_id }}
      project_field_values: ${{ inputs.project_field_values || format('[{{"project":{0},"field":"Status","value":"Review"}}]', vars.PROJECT_NUMBER) }}